export const WHOLE_UNITS_PATTERN = /^\d+$/;

export const LIVE_LOT_COUNT_NOTE = 'delivering, open and evicted lots all count';

export const EVICTED_BLOCK_NOTE =
    'an evicted lot keeps its per-seller slot until its remainder is on the way home, and it blocks every ' +
    'resource on that hub while it waits; other hubs are unaffected';
