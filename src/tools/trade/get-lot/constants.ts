export const GET_LOT_DESCRIPTION = [
    'Inspect a single lot by id — resource, remaining/listed units, price per unit, Hub location, seller, and',
    'lifecycle state. Public read. It resolves a lot in any state, including an evicted one; observing a lot is',
    'not the same as being able to buy it, and only an `open` lot is buyable.',
].join(' ');
