export const LIST_MY_LOTS_DESCRIPTION = [
    'List the lots you have created, across all lifecycle states (optionally filtered by `state`). Requires a',
    'session. Use this to find your lotIds and track listing and return progress. Evicted lots — thrown out of',
    'their hub by its owner, still yours, unbuyable, and owing a return home — are included by default and can',
    'be isolated with `state=evicted`.',
].join(' ');
