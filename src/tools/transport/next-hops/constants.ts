export { NEXT_HOPS_DESCRIPTION } from '../constants.js';

// Open ground makes every candidate list long; the summary stays readable while the payload stays complete.
export const NEXT_HOPS_SUMMARY_LIMIT = 20;
